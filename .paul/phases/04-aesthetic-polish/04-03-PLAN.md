---
phase: 04-aesthetic-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: false
---

<objective>
## Goal
Replace the About bio text with a restructured version: intro paragraph (engineering context + range), personal paragraph (hobbies/personality), and a "Currently" bullet list (roles).

## Purpose
The current bio reads like a résumé. The new version leads with who Ethan is as a person before listing what he's doing — making a more genuine first impression on visitors.

## Output
- Modified: `src/app/page.tsx` — AboutPanel bio paragraphs replaced with new content and structure
</objective>

<context>
## Source Files
@src/app/page.tsx
</context>

<acceptance_criteria>

## AC-1: Bio opens with engineering intro paragraph
```gherkin
Given the About tab is open
When the user reads the bio
Then the first paragraph introduces Ethan as a CE student at UWaterloo
And references the range of work (procrastination shamer → policy engine for enterprise software)
```

## AC-2: Personal paragraph follows with hobbies/personality
```gherkin
Given the About tab is open
When the user reads the second paragraph
Then it covers noodles, gym, EDM, trips, and MrHomeless
And the MrHomeless mention is a clickable external link
And there is no mention of engineering or technical work
```

## AC-3: Currently section is a bullet list
```gherkin
Given the About tab is open
When the user reads the third section
Then it shows "Currently:" followed by three bullet points
And each bullet has a bolded org name and brief descriptor
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Replace About bio content in AboutPanel</name>
  <files>src/app/page.tsx</files>
  <action>
    In the `AboutPanel` function, replace the three existing `<p>` bio paragraphs (and the Resume button) with the following structure. Keep everything else (photo, name, title, divider, Resume button, Technologies section) unchanged.

    New content to replace the three `<p>` elements:

    **Paragraph 1 — engineering intro:**
    ```
    I'm a Computer Engineering student at the University of Waterloo. I've built
    across the full stack — from a flashbang-wielding procrastination shamer to a
    policy engine for enterprise software.
    ```

    **Paragraph 2 — personal:**
    ```
    Outside of work, I'm a creature of simple pleasures and strong opinions —
    primarily about noodles, which I have been eating at a competitive level since
    birth. I hit the gym, chase good EDM (and occasionally wander into performative
    territory), and make a point to go on trips and retreats with friends whenever
    I can. I'm also the kind of person who gets genuinely excited about absurd tech
    projects — [MrHomeless] is my hero, inspiration, and role model.
    ```
    Where [MrHomeless] is: `<a href="https://www.youtube.com/@MrHomeless" target="_blank" rel="noopener noreferrer" className="text-accent hover:opacity-70 transition-opacity">MrHomeless</a>`

    **Paragraph 3 — currently (bullet list):**
    ```
    Currently:
    • Software Engineer Intern at Pratt & Whitney — developer tooling
    • Backend Developer at Hack the North
    • Project Developer at UW Blueprint — software for nonprofits
    ```
    Rendered as: a `<p>` with "Currently:" text, followed by a `<ul>` with three `<li>` elements. Org names bolded with `<strong>`.

    Keep the Resume button immediately after paragraph 3 as it currently is.

    Avoid: changing any styles, layout, or surrounding elements. Only replace the three `<p>` bio paragraphs.
  </action>
  <verify>
    `npx tsc --noEmit` passes with no errors.
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: all three content sections rendered correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured About bio — 3-part layout: intro, personal, currently bullets</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Check paragraph 1: CE student intro + procrastination shamer → policy engine
    4. Check paragraph 2: noodles, gym, EDM, trips, MrHomeless as clickable link
    5. Check paragraph 3: "Currently:" label + 3 bullet points with bolded org names
    6. Confirm: MrHomeless link opens YouTube in new tab
    7. Confirm: Resume button still present and functional
    8. Confirm: Technologies section below still intact
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Photo, name, title line, divider in AboutPanel
- Resume button (keep as-is, just reposition after new paragraphs)
- Technologies section and SkillsComponent
- All other panels (WorkPanel, ProjectsPanel)
- Any CSS or layout

## SCOPE LIMITS
- Bio text only — no structural or style changes
- Do not add new imports unless strictly necessary

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] All three bio sections render correctly
- [ ] MrHomeless link is functional
- [ ] No surrounding elements disturbed
- [ ] Human verification approved
</verification>

<success_criteria>
- Bio text replaced with new 3-paragraph structure
- MrHomeless renders as external link
- Currently section renders as bullet list with bolded orgs
- No TypeScript or layout regressions
- Human visual approval received
</success_criteria>

<output>
After completion, create `.paul/phases/04-aesthetic-polish/04-03-SUMMARY.md`
</output>
