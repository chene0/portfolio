---
phase: 01-content-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/skills.tsx
  - src/components/experiences/experience.tsx
  - src/content/portfolio.ts
  - src/app/page.tsx
autonomous: false
---

<objective>
## Goal
Fix the broken SkillsComponent in the experience card and add a Contact section with navbar entry.

## Purpose
Two concrete gaps exist before visual work begins:
1. `SkillsComponent` in `experience.tsx` always renders all 22 skills instead of the experience's own technologies — this must be fixed so that when tech stacks are added in Plan 01-02, they render correctly.
2. No Contact section or nav link exists, despite being a desired section.

## Output
- `SkillsComponent` accepts optional `skills` prop — renders all when absent, specific list when provided
- `experience.tsx` passes `experience.technologies` to `SkillsComponent`
- "Contact" added to `Sections[]` nav array
- Contact section added to `page.tsx` between Awards and the footer, containing email + social links with visual treatment
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/components/skills.tsx
@src/components/experiences/experience.tsx
@src/content/portfolio.ts
@src/app/page.tsx
@src/types/global.d.ts
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| react-expert | required | Before modifying React components | ○ |
| typescript-expert | required | Before modifying TypeScript types/props | ○ |

## Skill Invocation Checklist
- [ ] react-expert loaded
- [ ] typescript-expert loaded
</skills>

<acceptance_criteria>

## AC-1: SkillsComponent Accepts Optional Skills Prop
```gherkin
Given SkillsComponent is rendered without a skills prop
When the Skills section renders
Then all 22 skills from portfolio.ts are displayed (unchanged behavior)
```

## AC-2: Experience Card Renders Specific Technologies
```gherkin
Given an experience with a non-empty technologies array
When the experience card renders
Then only that experience's technologies are shown, not all 22 skills
```

## AC-3: Contact Section Exists and Is Reachable via Nav
```gherkin
Given the portfolio is loaded
When the user clicks "Contact" in the navbar
Then the page smoothly scrolls to a Contact section showing email and social links
```

## AC-4: Contact Section is Visually Distinct from Footer
```gherkin
Given the user is viewing the portfolio
When they reach the contact area
Then a full Contact section appears above the footer, with "Get in Touch" heading, email address displayed prominently, and GitHub/LinkedIn links
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update SkillsComponent to Accept Optional Skills Prop</name>
  <files>src/components/skills.tsx</files>
  <action>
    Refactor SkillsComponent to accept an optional `skills?: Skill[]` prop.
    - When `skills` prop is provided and non-empty, render those skills
    - When `skills` prop is absent or empty, fall back to the full `Skills` array from portfolio.ts (preserving existing behavior)
    - Keep the same badge/link rendering logic
    - The module-level `skills_element_array` pre-computation must be removed or moved inside the component since it now needs to be dynamic
    - The component should remain a named export `SkillsComponent`
    - Avoid: changing the visual output for the no-prop case
  </action>
  <verify>TypeScript compiles without errors: `npx tsc --noEmit`</verify>
  <done>AC-1 satisfied: SkillsComponent renders all skills when called with no props</done>
</task>

<task type="auto">
  <name>Task 2: Fix Experience Card to Pass Technologies to SkillsComponent</name>
  <files>src/components/experiences/experience.tsx</files>
  <action>
    Update the `SkillsComponent` call inside `experience.tsx` to pass `experience.technologies`:
    - Change `<SkillsComponent />` to `<SkillsComponent skills={experience.technologies} />`
    - No other changes to the component
    - Avoid: changing layout, styling, or any other logic
  </action>
  <verify>TypeScript compiles without errors: `npx tsc --noEmit`</verify>
  <done>AC-2 satisfied: Experience card will render specific technologies when populated in Plan 01-02</done>
</task>

<task type="auto">
  <name>Task 3: Add Contact Section to Portfolio</name>
  <files>src/content/portfolio.ts, src/app/page.tsx</files>
  <action>
    **In `src/content/portfolio.ts`:**
    - Add `{ title: "Contact", destination: "contact" }` to the end of the `Sections` array

    **In `src/app/page.tsx`:**
    - Add a Contact section between the Awards div and the footer
    - Use `id="contact"`
    - Content: heading "Get in Touch", a short line ("Feel free to reach out — I'm always open to new opportunities and conversations."), email address (e263chen@uwaterloo.ca) displayed as a mailto link styled prominently, and GitHub + LinkedIn icon links (reuse the `Socials` data from portfolio.ts — import it)
    - Style consistent with existing sections: `mx-10 lg:mx-40 my-16`, h2 heading
    - Display email and social links horizontally centered with gap, using the same icon link pattern as `SocialsComponent` but inline in the section
    - Avoid: creating a new component file — keep this inline in page.tsx
    - Avoid: removing or modifying the existing footer
  </action>
  <verify>
    1. `npx tsc --noEmit` passes
    2. "Contact" appears in the navbar
    3. Clicking Contact scrolls to the new section
  </verify>
  <done>AC-3 and AC-4 satisfied: Contact section exists with nav link and meaningful content</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. SkillsComponent now accepts optional skills prop
    2. Experience cards will render their own tech stack (not visible yet — will populate in Plan 01-02)
    3. New Contact section added between Awards and footer, with nav link
  </what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Confirm "Contact" appears in the navbar (desktop and mobile)
    4. Click "Contact" — verify smooth scroll to the new section
    5. Confirm the Contact section shows heading, email link, and social icons
    6. Confirm the Skills section still shows all 22 skills (no regression)
    7. Confirm experience cards are unchanged visually (technologies still empty)
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 01-02, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/app/api/getResumeSignedUrl/` — S3 signed URL logic untouched
- `src/types/global.d.ts` — no type changes needed for this plan
- `src/components/socials.tsx` — not modified; Contact section uses Socials data inline
- All existing section visual layouts

## SCOPE LIMITS
- Do not add a contact form (backend out of scope)
- Do not add new npm packages
- Do not change any other component styling

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with no errors
- [ ] `npm run build` succeeds (or `npm run dev` runs without errors)
- [ ] "Contact" visible in navbar (desktop + mobile)
- [ ] Contact section visible at bottom of page above footer
- [ ] Skills section still shows all 22 skills
- [ ] Human verify checkpoint approved
</verification>

<success_criteria>
- All 3 auto tasks completed
- Human verify approved
- No TypeScript errors
- No visual regressions on existing sections
</success_criteria>

<output>
After completion, create `.paul/phases/01-content-completion/01-01-SUMMARY.md`
</output>
