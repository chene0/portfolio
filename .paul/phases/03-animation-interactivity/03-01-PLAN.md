---
phase: 03-animation-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/skills.tsx
autonomous: false
---

<objective>
## Goal
Redesign the About panel with real bio content (Martin Sit-inspired layout) and upgrade the Skills section from 24 flat pills to categorized groups.

## Purpose
The About panel is the first thing visitors see. Currently it has a placeholder bio, a left-aligned layout that feels unfinished, and 24 undifferentiated skill pills. This plan replaces the placeholder with a real bio narrative and organizes skills by category — making the panel feel complete and intentional.

## Output
- `page.tsx`: AboutPanel with real bio text, improved visual composition
- `skills.tsx`: categorized view when rendered in About panel
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/app/page.tsx
@src/components/skills.tsx
@src/content/portfolio.ts
</context>

<acceptance_criteria>

## AC-1: Real Bio Content
```gherkin
Given the About tab is active
When the user views the About panel
Then they see a multi-paragraph bio (not the placeholder "I build software...") with real content about Ethan's role, work history, and background
```

## AC-2: Improved About Layout
```gherkin
Given the About tab is active
When the user views the panel on desktop
Then the content is centered with max-width ~640px, the photo is visually prominent, and the layout feels composed rather than left-anchored
```

## AC-3: Skills Categorized
```gherkin
Given the About tab is active
When the user views the Technologies section
Then skills are shown in 5 labeled category groups (Frontend / Backend / Languages / Data / Infra & Tools) instead of 24 undifferentiated pills
```

## AC-4: Per-Experience Skills Unaffected
```gherkin
Given any experience card renders tech stack pills
When skills are passed explicitly to SkillsComponent
Then they render flat (no categories), exactly as before
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AboutPanel with real bio + improved layout</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the AboutPanel function with a redesigned version:

    **Layout changes:**
    - Change outer div from `max-w-xl` to `max-w-2xl` — give the content room to breathe
    - Keep the photo+text flex-row on sm+ but increase the photo from 120px to 128px
    - Add a `mt-1` separator line below the subtitle: a small `<div className="w-8 h-px bg-border mt-3 mb-4" />`
    - The bio paragraphs should have `leading-relaxed` and `text-sm text-text-secondary`
    - Add a subtle horizontal rule `<hr className="border-border my-8" />` before the Technologies section

    **Bio content to replace the placeholder paragraphs:**
    ```
    I'm a Computer Engineering student at the University of Waterloo,
    currently interning at Pratt & Whitney on internal developer tooling.

    Previously a full-stack developer co-op at BTNX, shipping product
    across .NET, React, and AngularJS. On the side, I'm a backend
    developer at Hack the North and a project developer at UW Blueprint —
    building software for nonprofits.

    I work across the full stack, from TypeScript frontends to Python
    backends and C++ firmware. I've shipped projects at 5+ hackathons.
    Graduated with the UW President's Scholarship of Distinction.
    ```
    Render as three separate `<p>` elements with `mt-3` between them.

    **Do NOT change:** sidebar, WorkPanel, ProjectsPanel, mobile layout, TabNav, Socials.
  </action>
  <verify>
    Run `npm run build` — no errors.
    Visually: About panel shows 3 bio paragraphs, separator line, resume link, then hr, then Technologies.
  </verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Categorize skills in SkillsComponent</name>
  <files>src/components/skills.tsx</files>
  <action>
    Add a `categorized?: boolean` prop to SkillsComponent. When `true`, render skills in 5 labeled groups. When `false` or omitted, render flat as before.

    **Define the category groups inside skills.tsx** by importing the individual skill constants:
    ```ts
    import {
      SKILL_NEXTJS, SKILL_REACT, SKILL_ANGULARJS, SKILL_TAILWIND,
      SKILL_DOTNET, SKILL_EXPRESS, SKILL_FASTAPI, SKILL_SYMFONY, SKILL_PHP,
      SKILL_TYPESCRIPT, SKILL_PYTHON, SKILL_CSHARP, SKILL_CPP, SKILL_JAVA,
      SKILL_POSTGRES, SKILL_SQLSERVER, SKILL_MONGODB, SKILL_SQLALCHEMY, SKILL_PRISMA, SKILL_GRAPHQL,
      SKILL_DOCKER, SKILL_AWS, SKILL_PYTEST, SKILL_UNITY,
      Skills,
    } from '@/content/portfolio'
    ```

    **Category groups:**
    ```ts
    const SKILL_GROUPS = [
      { label: 'Frontend',      skills: [SKILL_NEXTJS, SKILL_REACT, SKILL_ANGULARJS, SKILL_TAILWIND] },
      { label: 'Backend',       skills: [SKILL_DOTNET, SKILL_EXPRESS, SKILL_FASTAPI, SKILL_SYMFONY, SKILL_PHP] },
      { label: 'Languages',     skills: [SKILL_TYPESCRIPT, SKILL_PYTHON, SKILL_CSHARP, SKILL_CPP, SKILL_JAVA] },
      { label: 'Data',          skills: [SKILL_POSTGRES, SKILL_SQLSERVER, SKILL_MONGODB, SKILL_SQLALCHEMY, SKILL_PRISMA, SKILL_GRAPHQL] },
      { label: 'Infra & Tools', skills: [SKILL_DOCKER, SKILL_AWS, SKILL_PYTEST, SKILL_UNITY] },
    ]
    ```

    **Categorized render:** Each group renders a category label (`text-text-muted text-xs uppercase tracking-widest mb-2`) above a `flex flex-wrap gap-2` row of pills. Groups separated by `mb-5`.

    **Flat render:** unchanged from current implementation.

    **In page.tsx:** Update `<SkillsComponent />` in AboutPanel to `<SkillsComponent categorized />`. All other usages (per-experience) pass explicit skills and remain flat.

    Do NOT change: the pill appearance, the flat rendering path, the `skills` prop logic.
  </action>
  <verify>
    Run `npm run build` — no type errors.
    Visually: About panel shows 5 labeled category sections. Experience tech stack pills still render flat.
  </verify>
  <done>AC-3 and AC-4 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>About panel redesigned with real bio + categorized skills</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Check About tab:
       - Bio shows 3 real paragraphs (not the placeholder "I build software...")
       - Separator line under subtitle, hr before Technologies
       - Skills show 5 labeled groups: Frontend / Backend / Languages / Data / Infra & Tools
    4. Check Work tab — experience tech stacks still render as flat pills (not categorized)
    5. Check mobile — layout still works
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/content/portfolio.ts` — data layer, no modifications this plan
- `src/types/global.d.ts` — type definitions unchanged
- `src/components/tab-nav.tsx` — navigation unchanged
- WorkPanel, ProjectsPanel, sidebar, mobile layout in page.tsx

## SCOPE LIMITS
- No animations in this plan (that's 03-03)
- No experience card expansion (that's 03-02)
- Skills categorization is About-panel only — experience cards stay flat

</boundaries>

<verification>
- [ ] `npm run build` passes with no errors
- [ ] About panel bio is 3 real paragraphs (not placeholder)
- [ ] Skills show 5 category groups in About panel
- [ ] Experience tech stacks still render flat (SkillsComponent with explicit skills prop = flat)
- [ ] Mobile layout unaffected
- [ ] No visual regressions in Work or Projects tabs
</verification>

<success_criteria>
- All tasks completed and human-verify checkpoint approved
- Build passes clean
- About panel looks composed and complete
- Skills categorization is readable and consistent with design tokens
</success_criteria>

<output>
After completion, create `.paul/phases/03-animation-interactivity/03-01-SUMMARY.md`
</output>
